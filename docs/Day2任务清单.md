# Day 2 任务清单与规划 — 2026-08-20（周四）· v3（交接版）

> 角色：大三实习生（ESP32-S3 路线）
> 阶段：**硬件到货状态待用户确认**；在未确认到货前，按「零硬件」路径执行。
> Mentor 验收节点：节点 1 立项完成 ✅；今日实际完成：**Arduino IDE 已可编译 ESP32-S3，VS Code 已下载**。其余项目未完成。
> 关联文档：hardware/bom.md ｜ docs/预研计划.md ｜ docs/需求文档.md

---

## 一、Day 1 收尾确认（已完成，不用重做）

- [x] 本地仓库 + 目录结构 + 4 份文档 + PlatformIO 骨架已建，GitHub 已 push（main = `12c1d81`）
- [x] BOM 已锁定实际型号，硬件已下单（全部待发货/在途）
- [x] 需求文档 / 预研计划 / 问题记录模板 已写

> 一句话：今天**不碰任何硬件**（一件都没到），全部精力放在：工具链能不能编译、方案还有没有坑、代码能不能先写出来。目标 = **板到那天，插上电就能开始测，而不是现场查资料**。

---

## 二、Day 2 目标（一句话）

> ① Arduino IDE 与 PlatformIO 都能编译 ESP32-S3 工程；② 整理 ESP32-S3、WS2812、I2C 和 SU-03T 的学习资料与待确认项；③ 把供电与电平方案保留为到货后的实测决策。今日不确定引脚、不修改硬件配置、不编写 M1 例程；未确认硬件到货前，全程零硬件依赖。

---

## 三、P0 必做（今日核心，6 项）

### 1. Arduino IDE 配置到能编译 ESP32-S3
> Arduino IDE 2.x 免费无激活码；"正确激活"= 配置到能开发 ESP32-S3。

- [x] 下载安装 Arduino IDE 2.x
- [x] 文件 → 首选项 → 附加开发板网址：
  `https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json`
- [x] 开发板管理器 → 搜 `esp32` → 安装（3.x 版本，支持 S3）
- [x] 选板 `ESP32S3 Dev Module`，**编译空 Blink**（无板也能 Compile，验证工具链）
- [ ] ⚠️ USB 驱动**先不装**：板没到，无法确认 USB 芯片（CH340 / CP210x / 原生 USB-CDC）。到货第一天再确认
- [x] 完成后截图 + commit：`chore: Arduino IDE 配置 ESP32-S3 完成`（commit 已随本次推送完成，截图自行补录）

### 2. VS Code + PlatformIO 装好，了解工程结构
- [x] 已下载 VS Code
- [ ] 安装 VS Code + PlatformIO 插件（尚未完成）
- [ ] 打开 `platformio.ini`（已建好：`board = esp32-s3-devkitc-1` + PSRAM + 4 库）
- [ ] 阅读 `platformio.ini` 与 `src/` 六模块职责，了解工程入口和配置文件位置
- [ ] 记录首次 Build 的前置条件：`src/config.h` 的引脚仍为「待定」，待硬件与引脚方案确认后再填入
- [ ] 如环境已装好，可尝试 PlatformIO: Build；若因占位符或工具链报错失败，仅记录问题，不在今天强行修复
- [ ] 完成后截图或记录 + commit：`docs: Day2 工具链与工程结构学习记录`

### 3. 登记电压模块已知信息（今天归档，不推断电平转换能力）
> 已知只有：模块可稳定输出 **3.3V / 5V / 12V**。目前没有型号、额定电流、通道数或逻辑电平转换资料，**不能据此认定它可以把 WS2812 DIN 从 3.3V 转为 5V**。

- [ ] 在 `hardware/bom.md` 记录已知输出：3.3V / 5V / 12V；型号、额定电流、通道数均待确认
- [ ] 到货后查看模块丝印/说明书，并用天宇 T21L 实测输出电压与带载能力
- [ ] 到货后再确认它是否具备**逻辑电平转换**功能；若不具备，另备 74HCT 系列等适合 WS2812 的电平转换方案

### 4. 细化供电方案（按模块实测能力分两条路）
> 背景：WS2812B 60 颗全白约 **3.6 A**。你买的电平转换模块 5V 输出通常是小电流（几百 mA ~ 1A 级），**大概率带不动 60 颗全白灯带**。

- [ ] **路线 A（优先，先测再说）**：到货后用万用表测模块 5V 输出带载能力 → 若 ≥2A，可供电主控 + OLED + SU-03T，灯带低亮度/少颗数运行
- [ ] **路线 B（稳妥）**：灯带**独立 5V/≥4A 电源**（BOM 已列待购），与主控 5V 模块/电平转换模块**共地不共线**
- [ ] 结论写进 `hardware/wiring.md`（今天先建文件占位，板到填图）
- [ ] 确认：DIN 串 **330Ω**（待购）、灯带电源端并 **100μF**（已购，闪烁/复位再加大到 470~1000μF）

### 5. 学习与资料整理：不确定引脚、不臆测协议
- [ ] 了解 ESP32-S3 引脚、strapping 引脚、I2C/UART 基本概念；今天不确定具体 GPIO
- [ ] 收集 SU-03T 商家资料入口；默认波特率、串口帧格式和词条工具均待资料/M6 实测确认
- [ ] 阅读需求文档中的 8 条语音命令映射，暂不编写解析代码

### 6. M1 前置学习（今天不写测试例程）
- [ ] 学习 Arduino/PlatformIO 的最小程序结构：`setup()`、`loop()`、库依赖与串口监视器
- [ ] 学习 WS2812 的供电、共地、GRB 颜色顺序、亮度限制等概念
- [ ] M1 例程（Blink、WS2812）推迟到完成上述学习或硬件到货后再写

### 7. 交接与远程同步

- [x] 完成项目交接：确认 M0 完成、M1 未开始、六模块仍为骨架
- [x] 将本地 Day 2 文档提交并推送到 GitHub `main`
- [x] 推送后核对 GitHub 最新 commit 与本地 `HEAD` 一致
- [ ] 如旧 WorkBuddy 对话中的 GitHub PAT 尚未撤销，在 GitHub 中撤销它；后续推送仅使用新的短期令牌或本机凭证

---

## 四、P1 本周可推进（板到前）

- [ ] SU-03T 8 条词条规划（命令 ID ↔ 动作，见需求 FR8）
- [ ] `hardware/wiring.md` 接线图初稿（ASCII/mermaid 先画，板到补照片）
- [ ] 每天 10 分钟站会自检（写进本文件底部）

---

## 五、建议引脚分配（初稿，待板复核）

> 原则：避开 S3 strapping 引脚（GPIO0/45/46 等）；任意 GPIO 可经矩阵做 I2C。

| 设备 | 建议 ESP32-S3 引脚 | 备注 |
|---|---|---|
| OLED SDA | GPIO 8 | I2C 0x3C/0x3D（SSD1315 兼容 SSD1306） |
| OLED SCL | GPIO 9 | |
| DS3231 SDA | GPIO 8 | 与 OLED 同 I2C 总线 |
| DS3231 SCL | GPIO 9 | |
| WS2812 DIN | GPIO 38 | 经电平转换模块 3.3V→5V；串 330Ω |
| 按键 MODE | GPIO 10 | 内部上拉 + 软件消抖 |
| 按键 ADD | GPIO 11 | |
| 按键 SUB | GPIO 12 | |
| 按键 OK | GPIO 13 | |
| 蜂鸣器 | GPIO 3 | 有源蜂鸣器，触发极性看模块 |
| SU-03T TX → ESP32 RX | GPIO 16 | UART2_RX，波特率见商家资料 |
| SU-03T RX ← ESP32 TX | GPIO 17 | UART2_TX |
| SU-03T GND | ESP32 GND | **必须共地** |

> ⚠️ 建议值，板到后用万用表/丝印确认无冲突（尤其避开已接 PSRAM/Flash 的引脚）。

---

## 六、SU-03T 协议预研要点（今日只收集，不臆测）

- 离线语音模块，UART 输出识别到的命令字
- 默认波特率多为 **9600**（以商家资料为准）
- 词条用厂家配置工具烧录，可自定
- 帧格式 / 校验 / 命令字编号 **M6 接 ESP32 前用电脑串口助手实测确认**
- 8 条命令映射（对齐需求 FR8）：

| 语音 | 命令 ID | 动作 |
|---|---:|---|
| 打开灯 | 0x01 | 打开当前灯光模式 |
| 关闭灯 | 0x02 | 关闭灯光 |
| 夜灯模式 | 0x03 | 低亮度暖光 |
| 暖光模式 | 0x04 | 暖光 |
| 阅读模式 | 0x05 | 白光 |
| 关闭闹钟 | 0x06 | 关闭当前闹钟 |
| 再睡五分钟 | 0x07 | 进入贪睡 |
| 开始日出 | 0x08 | 手动启动日出模拟 |

---

## 七、时间预算

| 任务 | 预计耗时 |
|---|---:|
| Arduino IDE 配置 + 编译空 Blink | 30 min |
| VS Code + PlatformIO + 骨架编译 | 30 min |
| 电平转换模块规格登记 + 供电方案细化 | 20 min |
| 引脚表 + SU-03T 资料收集 | 40 min |
| Arduino / PlatformIO / WS2812 基础学习 | 40~60 min |
| 文档同步、提交与远端核验 | 15 min |
| **合计** | **~2.5~3 h** |

---

## 八、执行顺序（建议）

```text
1. Arduino IDE + ESP32 包 → 编译空 Blink（30 min）
2. VS Code + PlatformIO → Build 骨架（30 min）
3. 电平转换模块规格登记 → 供电方案两条路写进 wiring.md（20 min）
4. 引脚表 + SU-03T 资料（40 min）
5. 学习 Arduino / PlatformIO / WS2812 基础（40~60 min）
6. 文档同步 → commit → push → 核验远端（15 min）
7. 站会自检写底部
```

---

## 九、到货第一天必做核对（提前写好，板到直接执行）

- [ ] 逐件拆包对照 BOM 核型号、数量
- [ ] 确认 ESP32-S3 板 USB 芯片 → 装对应驱动（CH340 / CP210x / 原生 CDC 免驱）
- [ ] **用天宇 T21L 测电平转换模块 5V 输出：空载电压、带载能力** → 定供电路线 A 或 B
- [ ] 万用表量 5V 模块输出电压是否稳定
- [ ] 烧录 01_blink → 板载 LED 闪烁 → 端口/工具链闭环

---

## 十、站会自检模板（今晚回填）

### 今天完成
- [x] Arduino IDE 配置 ESP32-S3 完成，能编译空 Blink
- [x] VS Code 已下载
- [ ] PlatformIO 插件安装与工程编译（未完成）
- [ ] 电压模块规格核对与供电方案整理（未完成）
- [ ] ESP32-S3 / WS2812 / SU-03T 基础学习（未完成）
- [ ] M1 例程（明确延后，尚未编写）
- [x] 本地与 GitHub `main` 已同步

### 遇到问题
- 暂无；今日仅完成 Arduino IDE 编译验证与 VS Code 下载。

### 解决方案
-

### 明天计划
- 安装 PlatformIO 插件并熟悉工程结构；继续学习 ESP32-S3 与 WS2812 基础。硬件到货后再执行「到货第一天核对清单」。

---

> Mentor 提醒：今天硬件一件没到，但别空等。工具链编译 + 方案细化 + 例程预写，这三样做完，板到当天就能直接烧 Blink、点灯。每完成一项 commit 一次。
